export const smtp_config = {
    host: process.env.SMTP_SERVER,
	port: process.env.SMTP_PORT,
	smtp_user: process.env.SMTP_USER,
	smtp_pwd: process.env.SMTP_PWD
}


export const Imsg_data = {
	from: "no-rely@doc-app.com",
}

